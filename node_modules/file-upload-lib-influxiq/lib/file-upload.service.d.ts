import { HttpClient, HttpHeaders } from '@angular/common/http';
export declare class FileUploadService {
    private httpClient;
    BASE_URL: string;
    httpOptions: {
        headers: HttpHeaders;
    };
    constructor(httpClient: HttpClient);
    upload(uploadURL: any, data: any): import("rxjs").Observable<string | {
        status: string;
        data: any;
    }>;
}
